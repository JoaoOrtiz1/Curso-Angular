'use strict'
var minhaVar = 'minha variavel';

var numeros = [1,2,3];
numeros.map((valor)=>{
    return valor*2;
})

class Math1{
    soma(x,y){
        return x+y;
    }
}

//ES6
const minhaFunction = (x, y)=>{
    return x+y;
}

